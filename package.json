{
  "name": "@sminnee/res-suspenders",
  "version": "0.0.7",
  "scripts": {
    "start": "rescript && node src/index.mjs",
    "dev": "rescript build -w",
    "clean": "rescript clean",
    "build": "rescript build",
    "test": "rescript && mocha",
    "test-watch": "mocha --watch --parallel",
    "format": "rescript format src/*.res"
  },
  "pre-commit": [
    "format"
  ],
  "keywords": [
    "rescript",
    "bucklescript"
  ],
  "author": "Sam Minnée <sam.minnee@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.5.0",
    "mocha": "^11.0.2",
    "pre-commit": "^1.2.2"
  },
  "dependencies": {
    "rescript": "^11.1.4"
  }
}
